# 心海法律 AI - 视觉识别配置指南

**创建时间**: 2026-05-18 18:00  
**创建人**: COO  
**版本**: V1.0

---

## 📊 配置总览

| 功能 | 服务商 | 方案 | 状态 |
|------|--------|------|------|
| **OCR 文字提取** | 阿里云 | Green SDK / HTTP API | ⚠️ 需配置 |
| **图片理解识别** | 百炼 | qwen-vl-max | ✅ 已配置 |
| **语音识别** | 阿里云 | NLS SDK | ✅ 已配置 |

---

## 1. 阿里云 OCR 配置 ⚠️

### 当前状态
- ✅ SDK 已安装：`aliyun-python-sdk-green==3.6.6`
- ✅ API Key 已配置：`ALIYUN_ACCESS_KEY_ID/SECRET`
- ❌ **需要开通服务**：阿里云 Green 内容安全服务
- ❌ **需要配置白名单**：服务器 IP

### 配置步骤

#### 步骤 1: 开通服务
1. 登录阿里云控制台：https://green.console.aliyun.com/
2. 开通"内容安全 Green"服务
3. 确认 OCR 功能可用

#### 步骤 2: 获取 API Key
- Access Key ID: 从 `.env` 环境变量读取 ✅
- Access Key Secret: 从 `.env` 环境变量读取 ✅

#### 步骤 3: 配置白名单
- 添加服务器 IP：`8.218.93.213`
- 允许 API 调用

#### 步骤 4: 测试调用
```python
from aliyunsdkcore.client import AcsClient
from aliyunsdkgreen.request.v20180509 import ImageSyncDetectRequest

client = AcsClient(
    os.getenv('ALIYUN_ACCESS_KEY_ID'),
    os.getenv('ALIYUN_ACCESS_KEY_SECRET'),
    'cn-shanghai'
)

request = ImageSyncDetectRequest.ImageSyncDetectRequest()
# 设置请求参数...
response = client.do_action_with_exception(request)
```

### 替代方案：使用 HTTP API

如果 SDK 配置复杂，可以直接调用 HTTP API：

```python
import requests
import base64
import hashlib
import hmac
from datetime import datetime

def ocr_aliyun_http(image_path):
    """阿里云 OCR HTTP API 调用"""
    access_key_id = os.getenv('ALIYUN_ACCESS_KEY_ID')
    access_key_secret = os.getenv('ALIYUN_ACCESS_KEY_SECRET')
    
    # 读取图片
    with open(image_path, 'rb') as f:
        image_data = base64.b64encode(f.read()).decode('utf-8')
    
    # API 端点
    url = 'https://green.cn-shanghai.aliyuncs.com/green/image/detection'
    
    # 构建请求体
    body = {
        "tasks": [{
            "data": {
                "image": image_data
            },
            "service": "ocr"
        }]
    }
    
    # 发送请求
    headers = {
        'Content-Type': 'application/json',
        'X-Green-AccessKeyId': access_key_id,
        # ... 添加签名
    }
    
    response = requests.post(url, json=body, headers=headers)
    return response.json()
```

---

## 2. 百炼图片识别 ✅

### 当前状态
- ✅ SDK 已安装：`openai==1.12.0`
- ✅ API Key 已配置：`DASHSCOPE_API_KEY`
- ✅ 代码已实现：`recognize_image_bailian()`
- ✅ 模型：`qwen-vl-max`

### 配置
```python
# .env
DASHSCOPE_API_KEY=sk-xxx

# config.yaml
ai_models:
  secondary:
    - name: qwen-vl-max
      provider: aliyun
      base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
      enabled: true
```

### 使用示例
```python
from openai import OpenAI

client = OpenAI(
    api_key=DASHSCOPE_API_KEY,
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)

response = client.chat.completions.create(
    model="qwen-vl-max",
    messages=[{
        "role": "user",
        "content": [
            {"image": "base64 图片数据"},
            {"text": "请描述这张图片的内容"}
        ]
    }]
)

image_description = response.choices[0].message.content
```

---

## 3. 我的图片识别能力

**是的，我（COO Agent）的图片识别能力使用的就是百炼大模型！**

### 配置位置
- 模型：`qwen-vl-max` 或 `qwen2-vl-72b-instruct`
- API: 通义千问视觉模型
- SDK: OpenAI 兼容接口

### 使用方式
当我需要识别图片时，会自动调用百炼大模型：
1. 读取图片并转换为 base64
2. 调用 qwen-vl 模型
3. 获取图片描述和文字内容

---

## 📋 待办事项

### 阿里云 OCR
- [ ] 开通阿里云 Green 服务
- [ ] 配置服务器 IP 白名单
- [ ] 测试 SDK 调用
- [ ] 或改用 HTTP API 方案

### 百炼图片识别
- [x] SDK 已安装
- [x] API Key 已配置
- [x] 代码已实现
- [ ] 生产模式测试

---

## 💰 费用对比

| 服务 | 计费方式 | 单价 | 备注 |
|------|---------|------|------|
| 阿里云 OCR | 按次计费 | ~0.05 元/次 | 纯文字提取 |
| 百炼图片识别 | 按 Token 计费 | ~0.02 元/次 | 内容理解 + 文字 |

**建议**: 
- 纯文字提取（身份证/票据）→ 阿里云 OCR
- 图片理解（场景/物体）→ 百炼大模型

---

**创建人**: COO  
**创建时间**: 2026-05-18 18:00  
**状态**: ⏳ 待配置阿里云 OCR 服务

---

*心海法律 AI · 视觉识别配置指南 | 版本：1.0 | 2026-05-18*
