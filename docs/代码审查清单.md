# 心海法律 AI 代码审查清单（V1.0）

**制定人**: 明鉴（审查官）  
**日期**: 2026-05-19  
**适用范围**: 所有代码提交

---

## 📋 审查流程

```
代码提交 → 自动检查 → 人工审查 → 问题反馈 → 修复验证 → 合并代码
```

---

## ✅ 自动检查（铁卫脚本）

### 1. 敏感信息扫描

```bash
# 执行命令
python3 /home/admin/xinhai_legal_api/scripts/pre_commit_secret_scan.py [文件路径]

# 通过标准
- 高危问题：0 个
- 中危问题：≤5 个
- 低危问题：≤10 个
```

**检查项**:
- [ ] API 密钥/密码/密钥
- [ ] 私钥/SSH 密钥
- [ ] 数据库连接字符串
- [ ] 微信/支付宝配置
- [ ] 手机号/身份证号

### 2. 语法检查

```bash
# 执行命令
python3 -m py_compile [文件路径]

# 通过标准
- 语法错误：0 个
```

### 3. 代码格式

```bash
# 执行命令
flake8 [文件路径]

# 通过标准
- 格式错误：≤10 个
- 行长度超标：≤5 个
```

---

## ✅ 人工审查（明鉴执行）

### 1. 功能正确性（30 分）

| 检查项 | 分值 | 检查方法 | 得分 |
|--------|------|---------|------|
| 需求覆盖 | 10 | 对照需求文档 | |
| 逻辑正确 | 10 | 代码逻辑审查 | |
| 边界处理 | 5 | 边界条件测试 | |
| 异常处理 | 5 | 异常场景测试 | |

**审查要点**:
- [ ] 是否实现所有需求功能
- [ ] 业务逻辑是否正确
- [ ] 边界条件是否处理（空值/极值）
- [ ] 异常情况是否有处理

**常见问题**:
```python
# ❌ 问题：缺少空值检查
def get_user_name(user):
    return user['name']  # user 可能为 None

# ✅ 修复
def get_user_name(user):
    if user is None:
        return None
    return user.get('name')
```

---

### 2. 代码规范（20 分）

| 检查项 | 分值 | 检查方法 | 得分 |
|--------|------|---------|------|
| 命名规范 | 5 | 人工审查 | |
| 代码格式 | 5 | flake8 检查 | |
| 注释完整 | 5 | 人工审查 | |
| 文档字符串 | 5 | 人工审查 | |

**审查要点**:
- [ ] 变量/函数命名是否清晰
- [ ] 代码格式是否符合规范
- [ ] 关键逻辑是否有注释
- [ ] 函数是否有文档字符串

**常见问题**:
```python
# ❌ 问题：命名不清晰
def func1(items):
    return sum(x['p'] for x in items)

# ✅ 修复
def calculate_total_price(items):
    """计算商品总价"""
    return sum(item['price'] for item in items)
```

---

### 3. 安全性（25 分）

| 检查项 | 分值 | 检查方法 | 得分 |
|--------|------|---------|------|
| 敏感信息 | 10 | 敏感信息扫描 | |
| SQL 注入 | 5 | 代码审查 | |
| XSS 防护 | 5 | 代码审查 | |
| 权限验证 | 5 | 代码审查 | |

**审查要点**:
- [ ] 是否有硬编码密钥/密码
- [ ] SQL 查询是否参数化
- [ ] 用户输入是否转义
- [ ] 接口是否有权限验证

**常见问题**:
```python
# ❌ 问题：SQL 注入风险
cursor.execute(f"SELECT * FROM users WHERE id = {user_id}")

# ✅ 修复
cursor.execute("SELECT * FROM users WHERE id = ?", (user_id,))

# ❌ 问题：缺少权限验证
def delete_user(user_id):
    db.delete_user(user_id)  # 任何人都能删除

# ✅ 修复
def delete_user(user_id, current_user):
    if not current_user.is_admin:
        raise PermissionError("需要管理员权限")
    db.delete_user(user_id)
```

---

### 4. 测试覆盖（15 分）

| 检查项 | 分值 | 检查方法 | 得分 |
|--------|------|---------|------|
| 单元测试 | 5 | 测试文件检查 | |
| 覆盖率 | 5 | coverage 报告 | |
| 边界测试 | 3 | 测试用例检查 | |
| 异常测试 | 2 | 测试用例检查 | |

**审查要点**:
- [ ] 是否有单元测试
- [ ] 测试覆盖率是否 ≥70%
- [ ] 是否测试边界条件
- [ ] 是否测试异常情况

**常见问题**:
```python
# ❌ 问题：测试覆盖不足
def test_normal():
    assert calculate_price([{'price': 100}]) == 100

# ✅ 修复：覆盖边界和异常
def test_empty_items():
    assert calculate_price([]) == 0

def test_invalid_discount():
    with pytest.raises(ValueError):
        calculate_price([], discount=1.5)
```

---

### 5. 性能影响（10 分）

| 检查项 | 分值 | 检查方法 | 得分 |
|--------|------|---------|------|
| 循环优化 | 3 | 代码审查 | |
| 数据库查询 | 3 | 代码审查 | |
| 缓存使用 | 2 | 代码审查 | |
| 资源释放 | 2 | 代码审查 | |

**审查要点**:
- [ ] 是否有低效循环
- [ ] 数据库查询是否优化
- [ ] 是否合理使用缓存
- [ ] 资源是否正确释放

**常见问题**:
```python
# ❌ 问题：N+1 查询
for order in orders:
    user = User.query.get(order.user_id)  # 每次查询数据库

# ✅ 修复：批量查询
user_ids = [order.user_id for order in orders]
users = User.query.filter(User.id.in_(user_ids)).all()
users_dict = {u.id: u for u in users}
```

---

## 📊 审查评分表

### 总分计算

```
总分 = 功能正确性 + 代码规范 + 安全性 + 测试覆盖 + 性能影响
     = ___/30  + ___/20   + ___/25  + ___/15   + ___/10
     = ___/100
```

### 评级标准

| 分数 | 评级 | 处理 |
|------|------|------|
| 90-100 | A (优秀) | 直接合并 |
| 80-89 | B (良好) | 修复小问题后合并 |
| 70-79 | C (合格) | 修复问题后重新审查 |
| 60-69 | D (需改进) | 大量修改后重新审查 |
| <60 | F (不合格) | 重新开发 |

---

## 📝 审查报告模板

```markdown
# 代码审查报告

**审查人**: 明鉴  
**审查日期**: 2026-__-__  
**审查文件**: [文件路径]  
**Git 提交**: [commit hash]

## 评分

| 检查项 | 得分 | 备注 |
|--------|------|------|
| 功能正确性 | __/30 | |
| 代码规范 | __/20 | |
| 安全性 | __/25 | |
| 测试覆盖 | __/15 | |
| 性能影响 | __/10 | |
| **总分** | **__/100** | **评级：__** |

## 发现的问题

### 高危问题 (必须修复)
1. [问题描述] @ [文件：行号]
   - 建议：[修复建议]

### 中危问题 (建议修复)
1. [问题描述] @ [文件：行号]
   - 建议：[修复建议]

### 低危问题 (可选修复)
1. [问题描述] @ [文件：行号]
   - 建议：[修复建议]

## 审查结论

- [ ] ✅ 通过（≥90 分）
- [ ] ⚠️ 条件通过（80-89 分）
- [ ] ❌ 不通过（<80 分）

**审查人签名**: 明鉴
```

---

## 🔧 自动化审查工具

### 敏感信息扫描

```bash
python3 /home/admin/xinhai_legal_api/scripts/pre_commit_secret_scan.py [文件路径]
```

### 语法检查

```bash
python3 -m py_compile [文件路径]
```

### 代码格式

```bash
flake8 [文件路径]
```

### 测试覆盖

```bash
pytest --cov=[模块路径] --cov-report=html
```

---

## 📋 审查记录

### 审查日志

| 日期 | 文件 | 审查人 | 分数 | 评级 | 状态 |
|------|------|--------|------|------|------|
| 2026-05-19 | token_optimizer.py | 明鉴 | 92 | A | ✅ 通过 |
| 2026-05-19 | code_indexer.py | 明鉴 | 88 | B | ✅ 通过 |
| 2026-05-19 | chroma_search.py | 明鉴 | 85 | B | ✅ 通过 |

---

**维护人**: 明鉴（审查官）  
**最后更新**: 2026-05-19  
**下次评审**: 2026-06-19（月度评审）
